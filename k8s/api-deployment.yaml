apiVersion: apps/v1
kind: Deployment
metadata:
  name: deksdenflow-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: deksdenflow-api
  template:
    metadata:
      labels:
        app: deksdenflow-api
    spec:
      containers:
        - name: api
          image: deksdenflow-core:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: DEKSDENFLOW_DB_URL
              valueFrom:
                secretKeyRef:
                  name: deksdenflow-secrets
                  key: db_url
            - name: DEKSDENFLOW_REDIS_URL
              valueFrom:
                secretKeyRef:
                  name: deksdenflow-secrets
                  key: redis_url
            - name: DEKSDENFLOW_API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: deksdenflow-secrets
                  key: api_token
          ports:
            - containerPort: 8010
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
          livenessProbe:
            httpGet:
              path: /health
              port: 8010
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8010
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: deksdenflow-api
spec:
  selector:
    app: deksdenflow-api
  ports:
    - name: http
      port: 80
      targetPort: 8010
