[pytest]
testpaths = tests
norecursedirs = projects runs worktrees demo_bootstrap .venv .git .mypy_cache .ruff_cache .pytest_cache
timeout = 120
markers =
    integration: opt-in tests hitting real running services
